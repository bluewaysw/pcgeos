#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = truetype
ASM_TO_OBJS     = truetype.asm
UI_TO_RDFS      =
SRCS            = truetype.asm truetypeChars.asm truetypeEC.asm\
                  truetypeEscape.asm truetypeInit.asm truetypeMetrics.asm\
                  truetypePath.asm truetypeWidths.asm fontcomUtils.asm\
                  fontcomEscape.asm truetypeConstant.def truetypeMacros.def\
                  truetypeVariable.def
OBJS            = truetype.obj
LOBJS           =

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
