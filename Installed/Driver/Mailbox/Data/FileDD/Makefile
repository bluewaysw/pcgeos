#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = FileDD
ASM_TO_OBJS     = fileddManager.asm
UI_TO_RDFS      =
SRCS            = filedd.asm fileddBody.asm fileddCode.asm fileddEntry.asm\
                  fileddManager.asm fileddRead.asm fileddUtils.asm\
                  fileddWrite.asm fileddConstant.def fileddVariable.def
OBJS            = fileddManager.obj
LOBJS           =

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
