#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = datax
ASM_TO_OBJS     = dataxManager.asm
UI_TO_RDFS      =
SRCS            = datax.asm dataxAppl.asm dataxBehaviors.asm dataxEC.asm\
                  dataxHelper.asm dataxManager.asm dataxPipeAPI.asm\
                  dataxUtils.asm dataxConstants.def
OBJS            = dataxManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = datax

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
