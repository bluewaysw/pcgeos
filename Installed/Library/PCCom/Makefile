#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = pccom
ASM_TO_OBJS     = pccomManager.asm
UI_TO_RDFS      =
SRCS            = pccom.asm pccomClient.asm pccomData.asm pccomFile.asm\
                  pccomFSel.asm pccomIntegrity.asm pccomManager.asm\
                  pccomScr.asm pccomStrings.asm pccomUtils.asm\
                  pccomConstants.def pccomMacro.def
OBJS            = pccomManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = pccom

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
