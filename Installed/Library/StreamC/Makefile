#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = streamc
ASM_TO_OBJS     = streamCManager.asm
UI_TO_RDFS      =
SRCS            = streamCDriver.asm streamCEntry.asm streamCManager.asm\
                  streamCParallel.asm streamCSerial.asm streamCStream.asm\
                  streamCUtils.asm streamCConstant.def streamCVariable.def
OBJS            = streamCManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = streamc

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
