#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = cgadget
ASM_TO_OBJS     =
UI_TO_RDFS      =
SRCS            = uiDI.goc uiTI.goc TimeInp.goc RptTrig.goc DateInp.goc\
                  externs.goh
OBJS            = uiDI.obj uiTI.obj TimeInp.obj RptTrig.obj DateInp.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = cgadget

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
