#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = extui
STATBAR         = StatBar.goc
TITLMKR         = TitlMkrC.goc
UTILS           = Utils.goc
HDDNBTN         = HddnBtn.goc
TABLE           = TblItemC.goc Table.goc TblItemC.goh
TREE            = tree.goc
UI_TO_RDFS      =
OBJS            = StatBar.obj TitlMkrC.obj Utils.obj HddnBtn.obj\
                  TblItemC.obj Table.obj tree.obj
COMMON          =
MODULES         =
CMODULES        = StatBar TitlMkr UTILS HddnBtn Table TREE
SRCS            = $(STATBAR) $(TITLMKR) $(UTILS) $(HDDNBTN) $(TABLE)\
                  $(TREE) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = extui

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
