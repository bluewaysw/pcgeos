#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = extui
UTILS           = Utils.goc
TITLMKR         = TitlMkrC.goc
TABLE           = Table.goc TblItemC.goc TblItemC.goh
TREE            = tree.goc
HDDNBTN         = HddnBtn.goc
STATBAR         = StatBar.goc
UI_TO_RDFS      =
OBJS            = Utils.obj TitlMkrC.obj Table.obj TblItemC.obj tree.obj\
                  HddnBtn.obj StatBar.obj
COMMON          =
MODULES         =
CMODULES        = UTILS TitlMkr Table TREE HddnBtn StatBar
SRCS            = $(UTILS) $(TITLMKR) $(TABLE) $(TREE) $(HDDNBTN)\
                  $(STATBAR) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = extui

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
