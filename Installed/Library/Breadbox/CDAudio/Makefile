#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = cdaudio
ASM_TO_OBJS     = cdaudio_asm.asm
UI_TO_RDFS      =
SRCS            = cdaudio_asm.asm cdaudio.goc cdaudio.goh
OBJS            = cdaudio_asm.obj cdaudio.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = cdaudio

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
