#!/bin/bash

# if we use swat "send" on impgraph it copies the file to SYSTEM
# but it is expected in SYSTEM\IMPEX... so this is a workaround
# script...

rm ~/gbuild/localpc/ensemble/system/impex/impgraph.geo
cp impgraphec.geo ~/gbuild/localpc/ensemble/system/impex/impgraph.geo
