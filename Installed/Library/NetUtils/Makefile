#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = netutils
ASM_TO_OBJS     = netUtilsManager.asm
UI_TO_RDFS      =
SRCS            = address.asm hugelmem.asm hugelmemC.asm hugelmemEC.asm\
                  netUtilsManager.asm queue.asm queueEC.asm random.asm\
                  hugelmem.def queue.def
OBJS            = netUtilsManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = netutils

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
