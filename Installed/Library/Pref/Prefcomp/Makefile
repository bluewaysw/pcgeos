#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = prefcomp
ASM_TO_OBJS     = prefcomp.asm
UI_TO_RDFS      = prefcomp.ui
SRCS            = prefcomp.asm prefcompSerialValue.asm prefcomp.def\
                  prefcomp.ui
OBJS            = prefcomp.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = prefcomp

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
