#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = HotSpot
ASM_TO_OBJS     =
UI_TO_RDFS      =
SRCS            = hserrors.h genHS.goc headbody.goc hotspot.goc hstext.goc\
                  hstextHS.goc hsutils.goc
OBJS            = genHS.obj headbody.obj hotspot.obj hstext.obj\
                  hstextHS.obj hsutils.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = hotspot

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
