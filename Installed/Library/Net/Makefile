#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = net
ASM_TO_OBJS     = net.asm
UI_TO_RDFS      =
SRCS            = alert.asm hecb.asm init.asm msg.asm net.asm netC.asm\
                  netDir.asm netEC.asm netEnum.asm netMessaging.asm\
                  netObject.asm netPrint.asm netServer.asm netSocket.asm\
                  netUserInfo.asm netUtils.asm semaphore.asm\
                  netConstant.def netVariable.def net.ui
OBJS            = net.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = net

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
