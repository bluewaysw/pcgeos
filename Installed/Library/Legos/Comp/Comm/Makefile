#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = comm
COMMON          = commLibrary.goc
SERIAL          = serialComponent.goc
UI_TO_RDFS      =
OBJS            = commLibrary.obj serialComponent.obj
COMMON          = commInternal.goh
MODULES         =
CMODULES        = Common Serial
SRCS            = $(COMMON) $(SERIAL) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/coolcomm.ldf
LIBNAME         = coolcomm

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
