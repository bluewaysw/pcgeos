#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = file
ASM_TO_OBJS     = fileC.asm
UI_TO_RDFS      =
SRCS            = fileC.asm fileBuffer.goc fileComponent.goc\
                  fileLibrary.goc fileSelector.goc fileInternal.goh
OBJS            = fileC.obj fileBuffer.obj fileComponent.obj\
                  fileLibrary.obj fileSelector.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/coolfile.ldf
LIBNAME         = coolfile

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
