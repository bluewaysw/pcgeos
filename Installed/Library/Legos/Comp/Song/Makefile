#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = song
ASM_TO_OBJS     =
UI_TO_RDFS      =
SRCS            = songLibrary.goc synthComponent.goc waveComponent.goc\
                  songInternal.goh
OBJS            = songLibrary.obj synthComponent.obj waveComponent.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/coolsong.ldf
LIBNAME         = coolsong

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
