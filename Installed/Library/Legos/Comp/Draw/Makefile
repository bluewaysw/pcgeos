#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = draw
COMMON          = drawLibrary.goc
DRAW            = drawComponent.goc
SPRITE          = spriteComponent.goc spriteContent.goc
UI_TO_RDFS      =
OBJS            = drawLibrary.obj drawComponent.obj spriteComponent.obj\
                  spriteContent.obj
COMMON          = drawInternal.goh
MODULES         =
CMODULES        = Common Draw Sprite
SRCS            = $(COMMON) $(DRAW) $(SPRITE) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/cooldraw.ldf
LIBNAME         = cooldraw

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
