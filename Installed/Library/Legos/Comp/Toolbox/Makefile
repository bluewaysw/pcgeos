#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = toolbox
COMMON          = toolboxLibrary.goc
CONTROL         = controlComponent.goc
MESSAGE         = messageComponent.goc
MODULE          = moduleComponent.goc
UI_TO_RDFS      =
OBJS            = toolboxLibrary.obj controlComponent.obj\
                  messageComponent.obj moduleComponent.obj
COMMON          = toolboxInternal.goh
MODULES         =
CMODULES        = Common Control Message Module
SRCS            = $(COMMON) $(CONTROL) $(MESSAGE) $(MODULE) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = toolbox

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
