#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = ent
ASM_TO_OBJS     = entManager.asm
UI_TO_RDFS      =
SRCS            = entApp.asm entEC.asm entMain.asm entManager.asm\
                  entMaster.asm entUtil.asm entVis.asm entConstant.def\
                  entMacro.def entVariable.def
OBJS            = entManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = ent

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
