#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = bent
ASM_TO_OBJS     = bentMouse.asm bentUtil.asm
UI_TO_RDFS      =
SRCS            = bentMouse.asm bentUtil.asm bentEmbed.goc\
                  bentInterpreter.goc bentMain.goc bentNode.goc\
                  bentWindow.goc bentArt.goh bentInternal.goh
OBJS            = bentMouse.obj bentUtil.obj bentEmbed.obj\
                  bentInterpreter.obj bentMain.obj bentNode.obj\
                  bentWindow.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = bent

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
