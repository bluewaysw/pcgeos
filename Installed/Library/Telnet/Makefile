#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = telnet
ASM_TO_OBJS     = telnetManager.asm
UI_TO_RDFS      =
SRCS            = telnetApi.asm telnetCommand.asm telnetConnection.asm\
                  telnetEC.asm telnetManager.asm telnetOption.asm\
                  telnetParser.asm telnetUtils.asm telnetConstant.def\
                  telnetMacro.def telnetVariable.def
OBJS            = telnetManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = telnet

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
