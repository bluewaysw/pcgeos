#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = rsftool
MAIN            = dialogclass.goc filebuf.goc fselsup.goc ftmodi.goc\
                  ftpath.goc rsftool.goc rsftool2.goc ftlocals.goh\
                  macros.goh
UI              = toolui.goc toolui.goh
UI_TO_RDFS      =
OBJS            = dialogclass.obj filebuf.obj fselsup.obj ftmodi.obj\
                  ftpath.obj rsftool.obj rsftool2.obj toolui.obj
COMMON          =
MODULES         =
CMODULES        = Main UI
SRCS            = $(MAIN) $(UI) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = rsftool

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
