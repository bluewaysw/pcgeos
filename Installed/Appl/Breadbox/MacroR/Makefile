#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = macror
MACRODEM        = macrodem.goc
MACROENG        = macroengManager.asm
UI_TO_RDFS      =
OBJS            = macrodem.obj
COMMON          = macroeng.h macroeng.def
MODULES         = MACROENG
CMODULES        = MACRODEM
SRCS            = $(MACRODEM) $(MACROENG) $(COMMON)
LOBJS           =

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
