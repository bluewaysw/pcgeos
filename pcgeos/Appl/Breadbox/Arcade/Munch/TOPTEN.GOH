@ifndef TOPTEN
@define TOPTEN 1

Boolean loadLevel(byte nr);
byte numOfLevel(void);
void loadOptions(void);
void saveOptions(void);

extern dword punkte; /*scores*/
extern byte  leben,level,restartLevel,maxLevel; /*Anzahl der Leben,Levelnr.,...*/
extern Boolean ton,spur,hintergrund;
extern int startX,startY;

@endif
