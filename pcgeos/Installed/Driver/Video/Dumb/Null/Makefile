#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = Null
ASM_TO_OBJS     = nullManager.asm
UI_TO_RDFS      =
SRCS            = nullEntry.asm nullManager.asm nullStringTab.asm\
                  DUMBCOMDEVINFO.ASM nullConstant.def
OBJS            = nullManager.obj
LOBJS           =

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
