#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = prefvid
ASM_TO_OBJS     = prefvid.asm
UI_TO_RDFS      = prefvid.ui
SRCS            = prefvid.asm prefvidDeviceList.asm prefvidDialog.asm\
                  prefvidGadgets.asm prefvid.def prefvidDeviceList.def\
                  prefvidDialog.def prefvidGadgets.def prefvid.ui
OBJS            = prefvid.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = prefvid

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
