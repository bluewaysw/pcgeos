#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = X10DRVR
ASM_TO_OBJS     = X10drvr.asm
UI_TO_RDFS      = DIALOG.UI
SRCS            = x10chg.asm X10drvr.asm X10init.asm X10send.asm x10ser.asm\
                  X10SLEEP.ASM X10strat.asm TIMECHIP.DEF dialog.ui
OBJS            = X10drvr.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = x10drvr

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
