#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = EXTUI
HDDNBTN         = HddnBtn.goc
STATBAR         = STATBAR.GOC
TABLE           = Table.goc TblItemC.goc TblItemC.goh
TITLMKR         = TitlMkrC.goc
TREE            = tree.goc
UTILS           = Utils.goc
UI_TO_RDFS      =
OBJS            = HddnBtn.obj STATBAR.obj Table.obj TblItemC.obj\
                  TitlMkrC.obj tree.obj Utils.obj
COMMON          =
MODULES         =
CMODULES        = HddnBtn StatBar TABLE TitlMkr TREE UTILS
SRCS            = $(HDDNBTN) $(STATBAR) $(TABLE) $(TITLMKR) $(TREE)\
                  $(UTILS) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = extui

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
