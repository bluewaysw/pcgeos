#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = CGadget
ASM_TO_OBJS     =
UI_TO_RDFS      =
SRCS            = DateInp.goc RptTrig.goc TimeInp.goc uiDI.goc uiTI.goc\
                  externs.goh
OBJS            = DateInp.obj RptTrig.obj TimeInp.obj uiDI.obj uiTI.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = cgadget

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
