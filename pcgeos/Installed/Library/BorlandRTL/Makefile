#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = borlandrtl
ASM_TO_OBJS     = borland.asm
UI_TO_RDFS      =
SRCS            = borland.asm f_lxmul.asm f_pcmp.asm f_scopy.asm h_ldiv.asm\
                  h_llsh.asm h_lrsh.asm h_lursh.asm h_pada.asm h_padd.asm\
                  h_pina.asm h_psbp.asm h_spush.asm n_lxmul.asm n_pcmp.asm\
                  n_scopy.asm
OBJS            = borland.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
