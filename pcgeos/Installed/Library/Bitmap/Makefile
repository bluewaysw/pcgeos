#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = bitmap
ASM_TO_OBJS     = bitmapMain.asm
UI_TO_RDFS      = uiManager.ui
SRCS            = backupProcess.asm bitmap.asm bitmapAnts.asm bitmapC.asm\
                  bitmapGState.asm bitmapKeyboard.asm bitmapMain.asm\
                  bitmapTransfer.asm bitmapUI.asm bitmapUndo.asm\
                  bitmapUtils.asm dragTool.asm drawEllipseTool.asm\
                  drawRectTool.asm ellipseTool.asm eraserTool.asm\
                  fatbits.asm fatbitsTool.asm floodFillTool.asm\
                  lineTool.asm pencilTool.asm pointerImages.asm\
                  rectTool.asm selectionTool.asm textTool.asm tool.asm\
                  uiFormatControl.asm uiToolControl.asm\
                  visTextForBitmaps.asm bitmapConstant.def myMacros.def\
                  fatbits.ui uiFormatControl.ui uiManager.ui\
                  uiToolControl.ui uiUndoStrings.ui
OBJS            = bitmapMain.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = bitmap

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
