#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = accpnt
ASM_TO_OBJS     = accpntManager.asm
UI_TO_RDFS      = accpnt.ui
SRCS            = accpntApi.asm accpntCApi.asm accpntControl.asm\
                  accpntList.asm accpntManager.asm accpntUtils.asm\
                  accpntClass.def accpntConstant.def accpntMacro.def\
                  accpnt.ui
OBJS            = accpntManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = accpnt

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
