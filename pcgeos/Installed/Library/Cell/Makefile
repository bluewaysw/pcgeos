#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = cell
ASM_TO_OBJS     = cellManager.asm
UI_TO_RDFS      =
SRCS            = cellC.asm cellCell.asm cellEC.asm cellFlags.asm\
                  cellInsertDelete.asm cellMain.asm cellManager.asm\
                  cellRange.asm cellRow.asm cellSort.asm cellUtils.asm\
                  cellConstants.def cellMacros.def
OBJS            = cellManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = cell

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
