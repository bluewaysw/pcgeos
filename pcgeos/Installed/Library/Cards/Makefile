#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = cards
ASM_TO_OBJS     = cardsMain.asm
UI_TO_RDFS      = cards.ui
SRCS            = card.asm cardsMain.asm deck.asm game.asm hand.asm\
                  uiCardBackSelector.asm myMacros.def cards.ui\
                  uiCardBackSelector.ui
OBJS            = cardsMain.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = cards

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
