#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = ssmeta
ASM_TO_OBJS     = ssmetaManager.asm
UI_TO_RDFS      = ssmetaStrings.ui
SRCS            = ssmetaC.asm ssmetaDataRecord.asm\
                  ssmetaInitAndExitCode.asm ssmetaMain.asm\
                  ssmetaManager.asm ssmetaUtils.asm ssmetaConstant.def\
                  ssmetaStrings.ui
OBJS            = ssmetaManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = ssmeta

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
