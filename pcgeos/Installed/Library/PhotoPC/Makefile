#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = PHOTOPC
ASM_TO_OBJS     =
UI_TO_RDFS      =
SRCS            = eph_cmd.c eph_err.c eph_io.c eph_iob.c eph_open.c\
                  eph_read.c photopc.c config.h eph_io.h eph_priv.h
OBJS            = eph_cmd.obj eph_err.obj eph_io.obj eph_iob.obj\
                  eph_open.obj eph_read.obj photopc.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = photopc

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
