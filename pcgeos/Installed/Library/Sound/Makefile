#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = sound
ASM_TO_OBJS     = soundManager.asm
UI_TO_RDFS      =
SRCS            = soundC.asm soundCommon.asm soundDriver.asm\
                  soundManager.asm soundMixer.asm soundResident.asm\
                  soundVoiceAllocation.asm soundConstant.def soundError.def
OBJS            = soundManager.obj
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = sound

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
