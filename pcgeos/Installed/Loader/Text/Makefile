#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = Text
ASM_TO_OBJS     = loader.asm
UI_TO_RDFS      =
SRCS            = loader.asm MAIN.ASM LOCATE.ASM STRINGS.ASM INI.ASM\
                  LOAD.ASM HEAP.ASM PATH.ASM VIDEODETECT.ASM
OBJS            = loader.obj
LOBJS           =

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
