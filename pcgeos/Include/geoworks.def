COMMENT @----------------------------------------------------------------------

	Copyright (c) GeoWorks 1992 -- All Rights Reserved

PROJECT:	PC/GEOS
FILE:		geoworks.def

REVISION HISTORY:
	Name	Date		Description
	----	----		-----------
	doug	1/92		Initial version

DESCRIPTION:

	Certain mechanisms within PC/GEOS utilize a two-word indentification
	scheme in which the first word is a ManufacturerID, & the second
	is an enumeration defined by the manufacturer identified by the
	first word.  This file contains the enumerations for Geoworks's
	own ManufacturerID of MANUFACTURER_ID_GEOWORKS.
	
	NOTE:  Any developer assigned a ManufacturerID should have a file
	       similar to this one, named after their own company, for the
	       purpose of defining the meaning of enumerations when that
	       ManufacturerID is used.

	WARNING:
	       When updating this file, it is imperative that the "C"
	       equivalent, CInclude/geoworks.h, be updated as well.

	ONE MORE THING:
	       NOTHING should be placed in this file that can be made to
	       reside in a more specialized include file.  Yes, that includes
	       Notification data block structures!  This is not a substitute
	       for a library-specific definition file;  Constants & structures
	       pertinent to a specific library belong in a specific include
	       file for that library.
	       
	$Id: geoworks.def,v 1.1 97/04/04 14:22:35 newdeal Exp $

------------------------------------------------------------------------------@

SetDef	__GEOWORKS

;
; These are the base values for the various GCN list types.  They should match
; the base message value of their respective classes.
;
FIRST_META_GCN_LIST_TYPE equ 0
FIRST_VIS_CONTENT_GCN_LIST_TYPE equ 4a00h
FIRST_GEN_APP_GCN_LIST_TYPE equ 6800h
	

COMMENT @%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     	MSG_META_NOTIFY & MSG_META_NOTIFY_WITH_DATA_BLOCK NotificationType enums
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

	MSG_META_NOTIFY & MSG_META_NOTIFY_WITH_DATA_BLOCK are general purpose
	notification messages, whose data format & meaning are defined by
	the "NotificationType" passed in the messages.

	This section contains the enumerations of the NT_type field of
	NotificationType for the case of NT_manuf = MANUFACTURER_ID_GEOWORKS.

	Name	Date		Description
	----	----		-----------
	doug	1/92		Initial version

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@


GeoWorksNotificationType	etype	word

GWNT_INK			enum GeoWorksNotificationType
					; Data block holds InkHeader<>
;
;	This is a type sent out with the data it has collected as ink.
;
;	Pass: BP - handle of ink data (series of points)
;		   if BP=0, we couldn't allocate memory to hold all the points,
;		   or the GWNT_INK notification was intercepted by an
;		   InputMonitor.
;
;		  NOTE: If a monitor intercepts MSG_META_NOTIFY_WITH_DATA_BLOCK
;			with GWNT_INK, it must still pass it on, but may
;			pass on BP=0 if it wants to consume the	ink data
;			itself.
;
;		   Format of data:
;
;			InkHeader <>
;			Point<>
;			Point<>
;			  .		;The high bit of the x coord is set
;			  .		; to denote the end of a line segment.
;			  .
;
;	The points are all in the screen coordinates - objects may want to
;	convert then into their own window coordinates using
;	WinUntransform().


	; Used for sending out notification of status change
	; in GenControl objects.

GWNT_GEN_CONTROL_NOTIFY_STATUS_CHANGE	enum GeoWorksNotificationType
				; Data type:  NotifyGenControlStatusChange


	; Type for the GenEditControl (cut/copy/paste)
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GWNT_SELECT_STATE_CHANGE	enum	GeoWorksNotificationType
				; Data type: NotifySelectStateChange

GWNT_UNDO_STATE_CHANGE		enum	GeoWorksNotificationType
				; Data type: NotifyUndoStateChange

	; These types are used by the style sheet control

GWNT_STYLE_CHANGE		enum	GeoWorksNotificationType
				; data type NotifyStyleChange
GWNT_STYLE_SHEET_CHANGE		enum	GeoWorksNotificationType
				; data type NotifyStyleSheetChange

	; High-level types for the text object
	; These types all use MSG_META_NOTIFY_WITH_DATA_BLOCK

GWNT_TEXT_CHAR_ATTR_CHANGE	enum	GeoWorksNotificationType
				; Data type: VisTextNotifyCharAttrChange
GWNT_TEXT_PARA_ATTR_CHANGE	enum	GeoWorksNotificationType
				; Data type: VisTextNotifyParaAttrChange
GWNT_TEXT_TYPE_CHANGE		enum	GeoWorksNotificationType
				; Data type: VisTextNotifyTypeChange
GWNT_TEXT_SELECTION_CHANGE	enum	GeoWorksNotificationType
				; Data type: VisTextNotifySelectionChange
GWNT_TEXT_COUNT_CHANGE		enum	GeoWorksNotificationType
				; Data type: VisTextNotifyCountChange

	; Low-level types for the text attribute objects
	; These types all use MSG_META_NOTIFY_WITH_DATA_BLOCK

GWNT_TEXT_STYLE_CHANGE		enum GeoWorksNotificationType
					; Data type: NotifyTextStyleChange
GWNT_FONT_CHANGE		enum GeoWorksNotificationType
					; Data type: NotifyFontChange
GWNT_POINT_SIZE_CHANGE		enum GeoWorksNotificationType
					; Data type: NotifyPointSizeChange
GWNT_FONT_ATTR_CHANGE		enum GeoWorksNotificationType
					; Data type: NotifyFontAttrChange
GWNT_JUSTIFICATION_CHANGE	enum GeoWorksNotificationType
					; Data type: NotifyJustificationChange
GWNT_TEXT_FG_COLOR_CHANGE	enum GeoWorksNotificationType
					; Data type: NotifyColorChange
GWNT_TEXT_BG_COLOR_CHANGE	enum GeoWorksNotificationType
					; Data type: NotifyColorChange
GWNT_TEXT_PARA_COLOR_CHANGE	enum GeoWorksNotificationType
					; Data type: NotifyColorChange
GWNT_TEXT_BORDER_COLOR_CHANGE	enum GeoWorksNotificationType
					; Data type: NotifyColorChange

GWNT_SEARCH_REPLACE_ENABLE_CHANGE	enum GeoWorksNotificationType
				; Data type: NotifySearchReplaceEnableChange

GWNT_SPELL_ENABLE_CHANGE		enum GeoWorksNotificationType
				; Data type: NotifySpellEnableChange

;-----------------------------------------------------------------------------
;	CHART LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------

GWNT_CHART_TYPE_CHANGE		enum GeoWorksNotificationType
; Notification sent out when chart type changes

GWNT_CHART_GROUP_FLAGS		enum GeoWorksNotificationType

GWNT_CHART_AXIS_ATTRIBUTES	enum GeoWorksNotificationType



;-----------------------------------------------------------------------------
;	GROBJ LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------

GWNT_GROBJ_CURRENT_TOOL_CHANGE		enum GeoWorksNotificationType
GWNT_GROBJ_BODY_SELECTION_STATE_CHANGE	enum GeoWorksNotificationType
GWNT_GROBJ_AREA_ATTR_CHANGE		enum GeoWorksNotificationType
GWNT_GROBJ_LINE_ATTR_CHANGE		enum GeoWorksNotificationType
GWNT_GROBJ_TEXT_ATTR_CHANGE		enum GeoWorksNotificationType
GWNT_GROBJ_BODY_INSTRUCTION_FLAGS_CHANGE	enum GeoWorksNotificationType

GWNT_GROBJ_GRADIENT_ATTR_CHANGE		enum GeoWorksNotificationType


;-----------------------------------------------------------------------------
;	RULER LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------

GWNT_RULER_TYPE_CHANGE	enum GeoWorksNotificationType
GWNT_RULER_GRID_CHANGE	enum GeoWorksNotificationType
GWNT_RULER_GUIDE_CHANGE	enum GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	BITMAP LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------

GWNT_BITMAP_CURRENT_TOOL_CHANGE		enum GeoWorksNotificationType
GWNT_BITMAP_CURRENT_FORMAT_CHANGE	enum GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	FLAT FILE LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------

GWNT_FLAT_FILE_FIELD_PROPERTIES_STATUS_CHANGE	enum GeoWorksNotificationType
GWNT_FLAT_FILE_FIELD_LIST_CHANGE		enum GeoWorksNotificationType
GWNT_FLAT_FILE_RCP_STATUS_CHANGE		enum GeoWorksNotificationType
GWNT_FLAT_FILE_FIELD_APPEARANCE_CHANGE		enum GeoWorksNotificationType
GWNT_FLAT_FILE_DUMMY_CHANGE_2			enum GeoWorksNotificationType
GWNT_FLAT_FILE_DUMMY_CHANGE_3			enum GeoWorksNotificationType


;-----------------------------------------------------------------------------
;	SPOOL LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_SPOOL_DOC_OR_PAPER_SIZE	enum GeoWorksNotificationType
				; Data type: PageSizeReport

;-----------------------------------------------------------------------------
;	VIEW CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GWNT_VIEW_STATE_CHANGE	enum	GeoWorksNotificationType
				; Data type: NotifyViewStateChange

;-----------------------------------------------------------------------------
;	INK CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_INK_HAS_TARGET	enum	GeoWorksNotificationType
				; Send with MSG_META_NOTIFY (bp = non-zero if
				; we have the target)

;-----------------------------------------------------------------------------
;	PAGE CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_PAGE_STATE_CHANGE	enum	GeoWorksNotificationType
				; Data type: NotifyPageStateChange

;-----------------------------------------------------------------------------
;	DOCUMENT CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_DOCUMENT_CHANGE	enum	GeoWorksNotificationType
				; Data type: NotifyDocumentChange

;-----------------------------------------------------------------------------
;	DISPLAY CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_DISPLAY_CHANGE	enum	GeoWorksNotificationType
				; Data type: NotifyDisplayChange
GWNT_DISPLAY_LIST_CHANGE enum	GeoWorksNotificationType
				; Data type: NotifyDisplayListChange

;-----------------------------------------------------------------------------
;	SPLINE LIBRARY NOTIFICATION TYPES		
;-----------------------------------------------------------------------------
 
GWNT_SPLINE_MARKER_SHAPE		enum GeoWorksNotificationType
GWNT_SPLINE_POINT			enum GeoWorksNotificationType
GWNT_SPLINE_POLYLINE			enum GeoWorksNotificationType
GWNT_SPLINE_SMOOTHNESS			enum GeoWorksNotificationType
GWNT_SPLINE_OPEN_CLOSE_CHANGE	enum GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	Move to tab section
;-----------------------------------------------------------------------------

GWNT_UNUSED_1				enum GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	SPREADSHEET CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_SPREADSHEET_ACTIVE_CELL_CHANGE enum	GeoWorksNotificationType
				; Data type: NotifySSheetActiveCellChange
GWNT_SPREADSHEET_EDIT_BAR_CHANGE enum	GeoWorksNotificationType
				; Data type: NotifySSheetEditBarChange
GWNT_SPREADSHEET_SELECTION_CHANGE enum	GeoWorksNotificationType
				; Data type: NotifySSheetSelectionChange
GWNT_SPREADSHEET_CELL_WIDTH_HEIGHT_CHANGE enum GeoWorksNotificationType
				; Data type NotifySSheetCellWidthHeightChange
GWNT_SPREADSHEET_DOC_ATTR_CHANGE	enum GeoWorksNotificationType
				; Data type NotifySSheetDocAttrChange
GWNT_SPREADSHEET_CELL_ATTR_CHANGE	enum GeoWorksNotificationType
				; Data type NotifySSheetCellAttrChange
GWNT_SPREADSHEET_CELL_NOTES_CHANGE	enum GeoWorksNotificationType

GWNT_SPREADSHEET_DATA_RANGE_CHANGE	enum GeoWorksNotificationType
				; Data type NotifySSheetDataRangeChange

;-----------------------------------------------------------------------------
;	FLOAT LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_FLOAT_FORMAT_CHANGE	enum	GeoWorksNotificationType
; Data type: NotifyFloatFormatChange

;-----------------------------------------------------------------------------
;	IMPEX MAPPING CONTROL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_MAP_APP_CHANGE		enum		GeoWorksNotificationType
GWNT_MAP_LIBRARY_CHANGE		enum		GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	TRANSFER NOTIFICATION TYPE
;-----------------------------------------------------------------------------

GWNT_TEXT_NAME_CHANGE		enum		GeoWorksNotificationType
; Data type: VisTextNotifyNameChange

;------------------------------------------------------------------------------
;	CARD LIBRARY NOTIFICATION TYPES
;------------------------------------------------------------------------------

GWNT_CARD_BACK_CHANGE	enum GeoWorksNotificationType

;-----------------------------------------------------------------------------
;	TO BE MOVED LATER 		
;-----------------------------------------------------------------------------



GWNT_EDITABLE_TEXT_OBJECT_HAS_FOCUS		enum GeoWorksNotificationType
		;Is sent with MSG_META_NOTIFY - BP = TextFocusFlags

GWNT_TEXT_CONTEXT				enum GeoWorksNotificationType

GWNT_TEXT_REPLACE_WITH_HWR			enum GeoWorksNotificationType
				;Data type - InkHeader and ink data, followed
				; by ReplaceWithHWRData structure

;------------------------------------------------------------------------------
;	HELP NOTIFICATION TYPES
;------------------------------------------------------------------------------

GWNT_HELP_CONTEXT_CHANGE			enum GeoWorksNotificationType
; Data type: NotifyHelpChange


;-----------------------------------------------------------------------------
;	TO BE MOVED LATER 		
;-----------------------------------------------------------------------------

GWNT_FLOAT_FORMAT_INIT				enum GeoWorksNotificationType
; Data type: NotifyFloatFormatChange

;------------------------------------------------------------------------------
;	HARD ICON BAR NOTIFICATION TYPES
;------------------------------------------------------------------------------
GWNT_HARD_ICON_BAR_FUNCTION			enum GeoWorksNotificationType
; When received by a GenApplication object, causes the indicated
; HardIconBarFunction to be performed.

GWNT_STARTUP_INDEXED_APP			enum GeoWorksNotificationType
; Starts up the associated application.

;-----------------------------------------------------------------------------
;	Not classified
;-----------------------------------------------------------------------------

GWNT_SPOOL_PRINTING_COMPLETE			enum GeoWorksNotificationType
; The spooler does not send this out, but rather delays
; MSG_META_SEND_CLASSED_EVENT's that are sent to it having this as the
; encapsulated message, until printing is compled.  Used in remote (IACP)
; printing.

GWNT_MODAL_WIN_CHANGE				enum GeoWorksNotificationType
; Notification that the modal status of the application has
; changed in some way, either becoming modal, or becoming
; non-modal, or simply a change in which window is modal.  This message is
; send to the GAGCNLT_MODAL_WIN_CHANGE list from within the handler for
; MSG_GEN_APPLICATION_NOTIFY_MODAL_WIN_CHANGE after the modal window status
; has been updated.

GWNT_SPREADSHEET_NAME_CHANGE			enum GeoWorksNotificationType
; Notification that a name has been added, deleted or changed.
;

GWNT_DOCUMENT_OPEN_COMPLETE			enum GeoWorksNotificationType
; The GenDocument does not send this out, but rather delays 
; MSG_META_SEND_CLASSED_EVENT's that are sent to it having this as the
; encapsulated message, until the document has either been opened (& is
; ready to be printed), or has had the open operation aborted somehow.
; Used in remote (IACP).  NOTE:  used for remote printing -- as of yet,
; there is no need for the document to delay this message, & so code to 
; do that is not present.

;-----------------------------------------------------------------------------
;	EMAIL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_EMAIL_SCAN_INBOX				enum GeoWorksNotificationType
; Notify an email geode that it should check for new mail.
;

GWNT_FOCUS_WINDOW_KBD_STATUS			enum GeoWorksNotificationType
;
; 	On pen systems, this GCN Notification is sent from focus windows
;	to the GAGCNLT_FOCUS_WINDOW_KBD_STATUS GCN List with
;	the NotifyFocusWindowKbdStatus structure, to tell the system what the
;	floating keyboard should do

;-----------------------------------------------------------------------------
;	ADDED 6/10/93
;-----------------------------------------------------------------------------

GWNT_TAB_DOUBLE_CLICK				enum GeoWorksNotificationType
;
;	Sent when a tab is double clicked on.  This is an inelegant solution
;	to allow backwards compatibility

GWNT_PAGE_INFO_STATE_CHANGE			enum GeoWorksNotificationType
;
;	Sent when page info (size, margin) changes.
;	Data type: NotifyPageInfoChange
;

GWNT_CURSOR_POSITION_CHANGE	enum		GeoWorksNotificationType
; Data type: VisTextCursorPositionChange


;-----------------------------------------------------------------------------
;	FAX NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_FAX_NEW_JOB_CREATED			enum GeoWorksNotificationType
;
; Notify the fax spooler that there is a new fax file created.
;

GWNT_FAX_NEW_JOB_COMPLETED			enum GeoWorksNotificationType
;
; Notify the fax spooler that the new fax file is completely generated.
;


;-----------------------------------------------------------------------------
;	PDA EMAIL NOTIFICATION TYPES
;-----------------------------------------------------------------------------

GWNT_EMAIL_DATABASE_CHANGE			enum GeoWorksNotificationType
;
; notify email app that database has changed
;

GWNT_EMAIL_STATUS_CHANGE                        enum GeoWorksNotificationType
;
; notify email app of current communications state
;

GWNT_EMAIL_PAGE_PANEL_UPDATE                    enum GeoWorksNotificationType
;
; ask ExpoPagePanel to enable or disable features
;

;-----------------------------------------------------------------------------
;	PCCOM LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_PCCOM_DISPLAY_CHAR                    enum GeoWorksNotificationType
;
; 	Sent when there is a character ready to be displayed
;	Data type: char
;
GWNT_PCCOM_DISPLAY_STRING                    enum GeoWorksNotificationType
;
; 	Sent when there is a string ready to be displayed
;	Data type: MemHandle of block containing the null-terminated string
;
GWNT_PCCOM_EXIT 	   		     enum GeoWorksNotificationType
;
; 	Sent when the remote machine has sent the exit command and
;	pccom has exited.
;	Data type: PCComReturnType from the call to PCCOMEXIT.
;


;-----------------------------------------------------------------------------
;	FONTS ADD DELETE NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_FONTS_ADDED 	   		     enum GeoWorksNotificationType
;
; 	Sent when fonts are added to the available list
;	list of FontIDs added passed as data
;		first word	# of FontID to follow
;				FontID
;				FontID
;				...

GWNT_FONTS_DELETED 	   		     enum GeoWorksNotificationType
;
;	Sent when fonts are deleted from the available list
;	list of FontIDs deleted passed as data
;		first word	# of FontID to follow
;				FontID
;				FontID
;				...

;-----------------------------------------------------------------------------
;	INK GESTURE NOTIFICATION
;-----------------------------------------------------------------------------
GWNT_INK_GESTURE			     enum GeoWorksNotificationType
;	
;	used to send gestures from the gesture callback to the target
;	of the ink.  The data returned with the notification depends
;	on the particular gesture callback
;	

GWNT_TEXT_REPLACE_GESTURE		     enum GeoWorksNotificationType
;
;	used to send the char in GT_CHAR gesture along with its range
;	from the HWR Grid to the GenControl which should send it off
;	to the appropriate text object
;
;	data type: ReplaceWithGTCharData

;-----------------------------------------------------------------------------
;	MAILBOX LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_MAILBOX_SEND_CONTEXT			enum GeoWorksNotificationType
;
;	Sent when the application wishes to change the default data object
;	to be sent by the MailboxSendControl. For example, when changing
;	from displaying the table-of-contents for a notebook to displaying
;	a page of the notebook, the application would send out this notification
;	to change from MOT_PAGE_RANGE to MOT_CURRENT_PAGE.
;
;	Data type: block beginning with MailboxSendContextNotification
;		   structure.
;
;	May also be MSG_META_NOTIFY, where bp is just the MailboxObjectType
;


;-----------------------------------------------------------------------------
;	HYPERLINK LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_PAGE_NAME_CHANGE			enum GeoWorksNotificationType
;
;	Sent when the current page name has changed
;
;	Data type: NotifyPageNameChange structure.
;

;----------------------------------------------------------------------
;	GAME LIBRARY
;----------------------------------------------------------------------
GWNT_GAME_STATUS_CHANGE			enum	GeoWorksNotificationType

;----------------------------------------------------------------------
;	HOTSPOT LIBRARY
;----------------------------------------------------------------------
GWNT_DELETE_VARIABLE_GRAPHIC		enum	GeoWorksNotificationType
;
;	Sent by HotSpot Library when a non-hotspot variable graphic
; 	has been deleted.
;
;	Data type:	NotifyDeleteVariableGraphic
;
;----------------------------------------------------------------------
;	ADDITIONAL TEXT LIBRARY NOTIFICATION TYPE
;----------------------------------------------------------------------
GWNT_TEXT_HYPERLINKABILITY_CHANGE	enum	GeoWorksNotificationType
;
;	Sent by either the text library or an embedded object library,
;	such as HotSpot, to indicate that the hyperlinkability of
;	the selection has changed. The selection may be text or an
;	object embedded in the text through association with a
;	C_GRAPHIC character.
;
;	Data type: VisTextNotifyHyperlinkabilityChange
;

;-----------------------------------------------------------------------------
;	CONTENT LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_CONTENT_CONTEXT_CHANGE			enum GeoWorksNotificationType
;
; Notification type used by the Content library to tell the navigation 
; controller that the context has changed.
; 
; Data type: NotifyContentContextChange
;

GWNT_CONTENT_BOOK_CHANGE			enum GeoWorksNotificationType
;
; Notification type used by the Content library to tell the navigation, 
; context, find, and send controllers that the book has changed.
;
; Data type: NotifyContentBookChange
;

;-----------------------------------------------------------------------------
;	ADDITIONAL HYPERLINK LIBRARY NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_HYPERLINK_STATUS_CHANGE			enum GeoWorksNotificationType
;
;	Notify the hyperlink controller of a change in the status of
;	hyperlinks. 
;
;	Data type: NotifyHyperlinkStatusChange structure.
;

;-----------------------------------------------------------------------------
;	MODEM LURKER NOTIFICATION TYPES
;-----------------------------------------------------------------------------
GWNT_BEGIN_FAX_CONNECTION			enum GeoWorksNotificationType
; Notification type used by the modem lurker to tell the fax receive geode to
; begin receiving a fax. The word of data passed is the SerialPortNum to use.
; (Used with MSG_META_NOTIFY not _WITH_DATA_BLOCK)
;

GWNT_BEGIN_DATA_CONNECTION			enum GeoWorksNotificationType
;
; Notification type used by the modem lurker to tell the data connection geode
; to begin receiving its data.  The word of data passed is the SerialPortNum to
; use.
; (Used with MSG_META_NOTIFY not _WITH_DATA_BLOCK)
;

GWNT_END_SERIAL_CONNECTION			enum GeoWorksNotificationType
;
; Notification sent by the data/fax receive geodes when they are done
; with the serial port.  This message can be sent over the
; GCNSLT_NOTIFY_SERIAL_LURKER GCNList, which the modem lurker
; subscribes to.  The word of data passed is the SerialPortNum that was
; being used.
; (Used with MSG_META_NOTIFY not _WITH_DATA_BLOCK)
;

GWNT_LOST_SERIAL_CONNECTION			enum GeoWorksNotificationType
;
; Notification sent by the data/fax receive geodes when they have unsuccesfuly
; tried to close then open the serial port (to become the owner).  The lurker
; will try to recover.  The word of data passed is the SerialPortNum that was
; lost.  (sent to the GCNSLT_NOTIFY_SERIAL_LURKER GCNList)
; (Used with MSG_META_NOTIFY not _WITH_DATA_BLOCK)
;

;----------------------------------------------------------------------
;	FAX CONTROLLER NOTIFICATION TYPES
;----------------------------------------------------------------------

GWNT_FAX_DESTINATION_CHANGE			enum	GeoWorksNotificationType
;
; 	Sent by the fax destination controller to its output via
; 	MSG_META_NOTIFY_WITH_DATA_BLOCK when something has changed.
;
;	Data type: FaxDestinationChange
;

GWNT_FAX_DIAL_ASSIST_CHANGE			enum	GeoWorksNotificationType
;
; 	Sent by the dial assist controller to its output when its apply
;	trigger is hit.
;
;	Data type: DialAssistControlInfo
;

GWNT_FAX_LAST_DESTINATIONS_CHANGE		enum	GeoWorksNotificationType
;
; 	Sent to the system list GCNSLT_FAX_INFO_FILE_CHANGE when the last
;	destinations list in the fax info file has been
; 	changed.  Anyone interested in the latest list of recent
; 	destinations should re-read the list from the fax info file.
;

GWNT_COVER_PAGE_CHANGE				enum	GeoWorksNotificationType
;
;	Used for communication between the cover page controller and its owner.
;	Sent by the controller to its output when the user hits the "Done"
;	trigger.
;
;	Data type: CPCDataBlock
;

GWNT_FOAM_DB_CHANGE				enum GeoWorksNotificationType
;
;	Sent out when a change is made on a foam database.
;
;	Data type: FoamDBChangeNotification
;

GWNT_INIT_FILE_CHANGE				enum	GeoWorksNotificationType
;
;	Sent to the system list GCNSLT_NOTIFY_INIT_FILE_CHANGE
;	if an INI-file category/key is modified that apps need
;	to know about immediately.  To avoid having every client
;	lock down a block and do string comparisons to see what
;	category/key changed, only an enumerated type is passed,
;	with one enumeration for every interesting combination.
;
;	Data type: InitFileEntry
;

GWNT_KEYBOARD_EVENT				enum	GeoWorksNotificationType
;
;	Send to the system list GCNSLT_NOTIFY_KEYBOARD_EVENT when
;	the user presses a key that UI objects might be interested
;	in.  Normally used only by specific UIs to display the
;	state of "sticky" keys (e.g. ALT, SHIFT) on devices that
;	support delayed operation of such keys.  Generally if you
;	want to know about a keypress, you should use an input
;	monitor or handle MSG_META_KBD_CHAR.
;
;	Data type:  ToggleState
;

;----------------------------------------------------------------------
;	NAVIGATE CONTROLLER NOTIFICATION TYPES
;----------------------------------------------------------------------
GWNT_NAVIGATE_ENTRY_CHANGE			enum	GeoWorksNotificationType
;
;   	Send to the GCN list GAGCNLT_NOTIFY_NAVIGATE_ENTRY_CHANGE
;   	whenever there is a change that occurs in the list of entries.
;   	This notification is used to inform the output object to do
;    	the 'right' thing after the entry change.  
;
;   	Data type:  NotifyNavContextChange
;

GWNT_NAVIGATE_DELETE_ENTRY		    enum	GeoWorksNotificationType
;
;   	Send to the GCN list GAGCNLT_NOTIFY_NAVIGATE_ENTRY_CHANGE
;   	whenever there is a deletion of entry in the list.  This
;   	notification is used to inform the output object that an
;   	element of the list has just been deleted.  
;
;   	Data type:  NotifyNavContextChange
;

;----------------------------------------------------------------------
;	NOT USE (feel free to reuse)
;----------------------------------------------------------------------

GWNT_UNUSED_2	enum	GeoWorksNotificationType
GWNT_UNUSED_3	enum    GeoWorksNotificationType
GWNT_UNUSED_4	enum    GeoWorksNotificationType
GWNT_UNUSED_5	enum	GeoWorksNotificationType


;----------------------------------------------------------------------
;	BENT - BUILD-TIME COMPONENT LIBRARY (UI BUILDER)
;----------------------------------------------------------------------
GWNT_CURRENT_COMPONENT_CHANGE	enum	GeoWorksNotificationType
;
;	Sent by the BuilderInterpreterClass object when the currently
;	selected component in the UI builder has changed.  Any controller
;	that somehow reflects status of a EntClass (Legos) component can
;	benefit from this notification.
;
;	In Data Block:	NotifyCurrentComponentChange (defined in gool.goh)
;

GWNT_CONTACT_LIST_CUR_SELECTION		enum	GeoWorksNotificationType
;
; 	Sent to the GCNSLT_CONTACT_LIST_SELECTION GCN list whenever the
;	selection changes, so all contact lists maintain the same selection.
;
;	Uses MSG_META_NOTIFY with BP = index of record selected
;

;----------------------------------------------------------------------
; CONTACT LOG EVENTS
;----------------------------------------------------------------------
;
; 	The following notification types are used by the controllers
; 	in the Contact Log Library. They are to notify any additions
; 	or deletions to their respective contact logs.
;
GWNT_CONTACT_LOG_CONTACT_LOG_CHANGE	enum	GeoWorksNotificationType
;
;	Pass: nothing
;

GWNT_CONTACT_LOG_RECENT_CALLS_CHANGE	enum	GeoWorksNotificationType
GWNT_CONTACT_LOG_RECENT_FAXES_CHANGE	enum	GeoWorksNotificationType
GWNT_CONTACT_LOG_RECENT_MESSAGES_CHANGE	enum	GeoWorksNotificationType
;
; 	Pass: bp = LogEntryDirection
;

;
;
; 	The following is available for logging messages to be displayed on
;	the next lid open, if the lid is closed.
;
;	Data type:	IndicatorLogEventParams (defined in foam.def)	
;
GWNT_INDICATOR_LOG_EVENT		enum	GeoWorksNotificationType

;----------------------------------------------------------------------
;  RESPONDER HARDWARE NOTIFICATIONS
;----------------------------------------------------------------------
GWNT_RESPONDER_NOTIFICATION		enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_RESPONDER_NOTIFICATIONS or GCNSLT_TRANSPARENT_
;	DETACH_FULL_SCREEN_EXCL with ResponderNotificationType defined
;	in Include/Internal/respondr.def.
;
;	The format of the message is as follows:
;
;	msg:	MSG_META_NOTIFY
;	pass:	cx	= ManufacturerID ( geoworks )
;		dx	= GWNT_RESPONDER_NOTIFICATION
;		bp	= ResponderNotificationType
;
;	ResponderNotificationType:
;	RNT_LID_OPEN	- lid is open, apps wake up if they were asleep
;	RNT_LID_CLOSED	- lid is closed, take appropriate actions
;			  (see RNT_SAVE_DATA)
;	RNT_PHONE_ON	- phone is on
;	RNT_PHONE_OFF	- phone is off
;	RNT_SAVE_DATA	- we are about to go to sleep
;			  save any data promptly or they will be lost
;
;	RNT_LID_OPEN/CLOSED and RNT_PHONE_ON/OFF are sent to GCNSLT_
;	RESPONDER_NOTIFICATIONS, and RNT_SAVE_DATA is sent to GCNSLT_
;	TRANSPARENT_DETACH_FULL_SCREEN_EXCL.
;

;----------------------------------------------------------------------
;  FOAM NOTIFICATIONS
;----------------------------------------------------------------------
GWNT_FOAM_AUTO_SAVE			enum	GeoWorksNotificationType
;  
;	Sent to to the GCNSLT_FOAM_NOTIFICATIONS GCN list whenever a
;       application loses the full screen excl.  This notification tells
;       objects that the application is going away and they should save
;       their data.
;

GWNT_FOAM_GAINED_FULL_SCREEN_EXCL	enum	GeoWorksNotificationType
GWNT_FOAM_LOST_FULL_SCREEN_EXCL		enum	GeoWorksNotificationType
;
;	Sent out when an application gains/loses the full screen excl.
;

;----------------------------------------------------------------------
;  ACCESS POINT NOTIFICATIONS
;----------------------------------------------------------------------
GWNT_ACCESS_POINT_CHANGE		enum	GeoWorksNotificationType
;
; sent to GCNSLT_ACCESS_POINT_CHANGE with MSG_META_NOTIFY_WITH_DATA_BLOCK
; block contains AccessPointChangeDescription
;

;----------------------------------------------------------------------
;  INDICATOR NOTIFICATIONS
;----------------------------------------------------------------------
GWNT_INDICATOR_REPLACE_ICON_GLYPH		enum	GeoWorksNotificationType
GWNT_INDICATOR_REPLACE_TEXT_GLYPH		enum	GeoWorksNotificationType
GWNT_INDICATOR_SET_ACTIVE_CALL		   	enum	GeoWorksNotificationType
GWNT_INDICATOR_SET_NUM_UNREAD_DOCUMENTS	   	enum	GeoWorksNotificationType
GWNT_INDICATOR_CHANGE_NUM_UNREAD_DOCUMENTS	enum	GeoWorksNotificationType
GWNT_INDICATOR_CLEAR_NUM_UNREAD_DOCUMENTS	enum	GeoWorksNotificationType
GWNT_INDICATOR_SET_NUM_OUTGOING_DOCUMENTS	enum	GeoWorksNotificationType
GWNT_INDICATOR_CHANGE_NUM_OUTGOING_DOCUMENTS	enum	GeoWorksNotificationType
GWNT_INDICATOR_SET_CUSTOM_NOTE			enum	GeoWorksNotificationType
GWNT_INDICATOR_SET_MAIN_WINDOW_VISIBILITY	enum	GeoWorksNotificationType

;----------------------------------------------------------------------
;  RESPONDER FAX NOTIFICIATIONS
;----------------------------------------------------------------------
GWNT_VIEWER_DOCUMENT_STATUS_CHANGE		enum	GeoWorksNotificationType
;
;	Sent to GAGCNLT_VIEWER_NOTIFICATIONS when a file has been loaded.
;	Allows UI in transport drivers or what have you to reflect the current
;	file type (text/faxfile), resolution (standard/fine for faxes), or
;	whatever. 
;
;	Data type: ViewerDocumentStatus (defined in viewer.def)	
;

GWNT_PCMCIA_CARD_CHANGE			enum	GeoWorksNotificationType
;
;  Sent to the GCNSLT_PCMCIA_NOTIFY_CARD_CHANGE GCN list whenever a
;  card is inserted or removed.
;
;	Pass: bp.high = PCMCardChangeFlags, bp.low = socket number
;


;----------------------------------------------------------------------
;  INDICATOR NOTIFICATIONS cont'd
;----------------------------------------------------------------------
GWNT_INDICATOR_REFRESH_NOTIFY			enum 	GeoWorksNotificationType

;------------------------------------------------------------------------
; SECURITY SETTINGS NOTIFICATIONS
;-------------------------------------------------------------------------
; The following are sent to the GCNSLT_SECURITY_SETTINGS_NOTIFICATION
; list, when the security settings controller values are changed.
;
GWNT_SECURITY_PIN_CODE_REQUEST		enum	GeoWorksNotificationType
;
;  Pass: bp = TRUE/FALSE (on/off)
;
GWNT_SECURITY_LOCK_TIME_OUT		enum	GeoWorksNotificationType
;
; Pass: bp = time in minutes
;
GWNT_SECURITY_LEVEL			enum	GeoWorksNotificationType
;
; Pass: bp = TRUE/FALSE (on/off)
;

;------------------------------------------------------------------------
; DATASTORE CHANGE NOTIFICATION
;-------------------------------------------------------------------------
GWNT_DATASTORE_CHANGE			enum	GeoWorksNotificationType
;
;   	Send to the GCN list GAGCNLT_NOTIFY_DATASTORE_CHANGE
;   	whenever a datastore file is modified.
;
;   	Data type:  DataStoreChangeNotification (defined in datastore.def)
;

;----------------------------------------------------------------------
;  ANOTHER RESPONDER FAX NOTIFICIATION
;----------------------------------------------------------------------
GWNT_VIEWER_SHUTTING_DOWN		enum	GeoWorksNotificationType
;
;	Sent to GAGCNLT_VIEWER_NOTIFICATIONS when a viewer is shutting down.
;	Allows UI in transport drivers or what have you to clear themselves
;	if need be.
;
;	Data type: None.
;

;----------------------------------------------------------------------
;  INDICATOR NOTIFICATIONS cont'd
;----------------------------------------------------------------------
GWNT_INDICATOR_SET_TERMINATE_HANDLING	enum 	GeoWorksNotificationType

GWNT_INDICATOR_PUT_UP_FLASHING_NOTE	enum 	GeoWorksNotificationType
;
; Pass: IndicatorFlashingNote
;

GWNT_INDICATOR_PUT_UP_INFORMATION_NOTE	enum 	GeoWorksNotificationType
;
; Pass: IndicatorInformationNote
;

GWNT_INDICATOR_HANDLE_ECI_CMT_TERMINATE_STATUS \
					enum 	GeoWorksNotificationType
GWNT_INDICATOR_HANDLE_ECI_NETWORK_TERMINATE_STATUS \
					enum 	GeoWorksNotificationType

;------------------------------------------------------------------------
; SECURITY SETTINGS NOTIFICATIONS cont'd
;-------------------------------------------------------------------------
GWNT_SECURITY_SYSTEM_LOCKED		enum	GeoWorksNotificationType
;
; Pass: bp = TRUE/FALSE (locked/unlocked)
;

;---------------------------------------------------------------------------
; more FOAM NOTIFICATIONS
;---------------------------------------------------------------------------
GWNT_FOAM_IGNORE_BRING_TO_TOP		enum	GeoWorksNotificationType
;
; Description:
;	Sent out to inform interested applications to start/stop
;	ignoring MSG_GEN_BRING_TO_TOP to avoid launching in front
; 	of the current app.  Recipient should force queue themselves
;	a MSG_GEN_LOWER_TO_BOTTOM in case the app is already on top.
;	TRUE to begin ignoring, FALSE to stop ignoring.
;
; Pass:		bp = TRUE/FALSE
;

;--------------------------------------------------------------------------
; more Responder viewer notifications
;--------------------------------------------------------------------------
GWNT_VIEWER_DOCUMENT_CHANGE		enum	GeoWorksNotificationType
;
;	Sent to GAGCNLT_VIEWER_NOTIFICATIONS when a viewer has changed
;	to a new document from what it originally started with,
;	usually after converting a read-only document.
;
;	Data type: DocumentInfo
;

;-----------------------------------------------------------------------------
;	INK DIGITIZER COORDINATES NOTIFICATION
;-----------------------------------------------------------------------------
GWNT_INK_DIGITIZER_COORDS	enum GeoWorksNotificationType
				; Data type: InkDigitizerCoordsHeader
;
;	Notification sent by the InkInputMonitor with the digitizer
;	coordinates it has collected as ink. 
;
;	Pass: BP - handle of digitizer coordinate data 
;		   (series of digitizer coordinates and time stamps)
;		   if BP=0, we couldn't allocate memory to hold all the 
;		   points, or the GWNT_INK_DIGITIZER_COORDS notification 
;		   was intercepted by an InputMonitor.
;
;		  NOTE: If a monitor intercepts MSG_META_NOTIFY_WITH_DATA_BLOCK
;			with GWNT_INK_DIGITIZER_COORDS, it must still 
;			pass it on, but may pass on BP=0 if it wants 
;			to consume the ink data itself.
;
;		   Format of data:
;
;			InkDigitizerCoordsHeader<>
;			InkPoint<>
;			InkPoint<>
;			  .		;The high bit of the x coord is set
;			  .		; to denote the end of a line segment.
;			  .
;			  .
;
;	The points are all absolute digitizer coordinates.
;

;--------------------------------------------------------------------------
; Lizzy viewer notifications
;--------------------------------------------------------------------------
GWNT_VIEWER_LOAD_UNREAD_DOCUMENT	enum	GeoWorksNotificationType
;
;	Sent to GAGCNLT_VIEWER_NOTIFICATIONS usually through an IACP
;	connection to tell the object to load a new document into the
;	viewer.
;

;---------------------------------------------------------------------------
; PPP notifications
;---------------------------------------------------------------------------

GWNT_LOGIN_INITIALIZE			enum	GeoWorksNotificationType
;
; Description:
;	Sent via MSG_META_NOTIFY_WITH_DATA_BLOCK to inform a PPP
;	external login app to prepare for making a connection
;	to a remote host.  This is done before the call is made.
;
; Datatype: LoginInitInfo (Internal/login.def)
;

GWNT_LOGIN_ATTACH			enum	GeoWorksNotificationType
;
; Description:
;	Instructs an external login application to begin its
;	login sequence over an already open serial port.  Used by PPP
;	to allow manual/scripted logins over a modem port that PPP has
;	created.  Uses MSG_META_NOTIFY_WITH_DATA_BLOCK, sent to
;	the app object.  (if using IACP, use TO_SELF
;	TravelOption)
;
; Datatype: LoginAttachInfo (Internal/login.def)
;

GWNT_LOGIN_DETACH			enum	GeoWorksNotificationType
;
; Description:
;	Instructs login application to stop the login sequence started
;	with GWNT_LOGIN_INITIALIZE.  The caller is responsible
;	for closing the port, so login app should leave it alone.
;	The login app must also notify the protocol driver (via
;	the callback routine) that it is finished using the port, and
;	it is safe for the protocol to read/write to the port.
;	Uses MSG_META_NOTIFY, sent to the app object.
;	(if using IACP, use TO_SELF TravelOption)
;
; Pass:		bp  = Connection token passed in LII_connection
;			(via GWNT_LOGIN_INITIALIZE)
;


;---------------------------------------------------------------------------
; PENELOPE hardware notifications
;---------------------------------------------------------------------------
GWNT_PENELOPE_HARDWARE_NOTIFICATION	enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_PENELOPE_HARDWARE_NOTIFICATIONS or GCNSLT_TRANSPARENT_
;	DETACH_FULL_SCREEN_EXCL with PenelopeNotificationType defined
;	in Include/Internal/Penelope/peneGCN.def.
;
;	The format of the message is as follows:
;
;	msg:	MSG_META_NOTIFY
;	pass:	cx	= ManufacturerID ( geoworks )
;		dx	= GWNT_PENELOPE_HARDWARE_NOTIFICATION
;		bp high = PenelopeNotificationStatus
;		bp low	= PenelopeNotificationType
;
;	All notification types, with the exception of PNT_SAVE_DATA,
;	are sent to GCNSLT_PENELOPE_HARDWARE_NOTIFICATIONS.  The PNT_SAVE_DATA
;	notification is sent to GCNSLT_TRANSPARENT_DETACH_FULL_SCREEN_EXCL.
;
;	See Include/Internal/Penelope/peneGCN.def for details on these
;	data structures.
;

;---------------------------------------------------------------------------
; PENELOPE radio status notifications
;---------------------------------------------------------------------------
GWNT_PENELOPE_RADIO_STATUS_NOTIFICATION	enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_PENELOPE_RADIO_STATUS_NOTIFICATIONS with
;	PenelopeRadioStatus (Include/Internal/Penelope/peneGCN.def).
;
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID ( geoworks)
;		dx	= GWNT_PENELOPE_RADIO_STATUS_NOTIFICATION
;		bp	= PenelopeRadioStatus
;

;---------------------------------------------------------------------------
; PPP status notifications
;---------------------------------------------------------------------------
GWNT_PPP_STATUS_NOTIFICATION	enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_PENELOPE_RADIO_STATUS_NOTIFICATIONS with
;	PenelopeRadioStatus (Include/Internal/Penelope/peneGCN.def).
;
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID ( geoworks)
;		dx	= GWNT_PPP_STATUS_NOTIFICATION
;		bp	= PPPStatus
;

;---------------------------------------------------------------------------
; Confirm field change
;---------------------------------------------------------------------------
GWNT_CONFIRM_FIELD_CHANGE	enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_FIELD_CHANGE_NOTIFICATIONS.  When this notification
;	is received, receiver must call UserConfirmFieldChange as described
;	in ui.def in order to allow this field change to proceed (or not).
;
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (geoworks)
;		dx	= GWNT_CONFIRM_FIELD_CHANGE
;

;---------------------------------------------------------------------------
; Parental contrl change
;---------------------------------------------------------------------------
GWNT_PARENTAL_CONTROL_CHANGE	enum	GeoWorksNotificationType
;
;	Sent to GCNSLT_PARENTAL_CONTROL_CHANGE.
;
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (geoworks)
;		dx	= GWNT_PARENTAL_CONTROL_CHANGE
;		bp	= new parental control AccessFlags
;

;---------------------------------------------------------------------------
; Screen saver changes
;---------------------------------------------------------------------------
GWNT_SCREEN_SAVER_STATUS_NOTIFICATION	enum GeoWorksNotificationType
;
;	Sent to GCNSLT_SCREEN_SAVER_NOTIFICATIONS with ScrSaverState
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (MANUFACTURER_ID_GEOWORKS)
;		dx	= GWNT_SCREEN_SAVER_STATUS_NOTIFICATION
;		bp	= ScreenSaverStatus
;

;---------------------------------------------------------------------------
; Parental control website list change
;---------------------------------------------------------------------------
GWNT_PARENTAL_CONTROL_WEBSITE_LIST_CHANGE	enum GeoWorksNotificationType
;
;	Sent to GCNSLT_PARENTAL_CONTROL_WEBSITE_LIST_CHANGES
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (MANUFACTURER_ID_GEOWORKS)
;		dx	= GWNT_PARENTAL_CONTROL_WEBSITE_LIST_CHANGE
;		bp	= ParentalControlWebsiteListChangeType
;

;---------------------------------------------------------------------------
; Instant suspend/resume notifications
;---------------------------------------------------------------------------
GWNT_INSTANT_SUSPEND_RESUME_NOTIFICATION	enum GeoWorksNotificationType
;
;	Sent to GCNSLT_INSTANT_SUSPEND_RESUME_NOTIFICATIONS
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (MANUFACTURER_ID_GEOWORKS)
;		dx	= GWNT_INSTANT_SUSPEND_RESUME_NOTIFICATION
;		bp	= InstantSuspendResumeType
;

;---------------------------------------------------------------------------
; TCPIP status notifications
;---------------------------------------------------------------------------
GWNT_TCPIP_LINK_STATUS	enum GeoWorksNotificationType
;
;	Sent to GCNSLT_TCPIP_STATUS_NOTIFICATIONS
;	MSG:	MSG_META_NOTIFY
;	Pass:	cx	= ManufacturerID (MANUFACTURER_ID_GEOWORKS)
;		dx	= GWNT_TCPIP_LINK_STATUS
;		bp	= TcpipStatusNotificationType
;

GWNT_TCPIP_DHCP_STATUS	enum GeoWorksNotificationType
;
;	Sent to GCNSLT_TCPIP_STATUS_NOTIFICATIONS
;	MSG:	MSG_META_NOTIFY_WITH_DATA_BLOCK
;	Pass:	cx	= ManufacturerID (MANUFACTURER_ID_GEOWORKS)
;		dx	= GWNT_TCPIP_LINK_STATUS
;		bp	= MemHandle
;


COMMENT @%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	GenApplication GCNList enums
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

	The UI library's GenApplicationClass supports its very own GCN
	(General Change Notification) system separate from the kernel's.
	Lists within this system are identified by a GAGCNListType, whose
	enumerations are seperate from that of the kernel's GCN system.

	This section contains the enumerations of the GCNLT_type field for
	GCNListType used within a GenApplication for the case of
	GCNLT_manuf = MANUFACTURER_ID_GEOWORKS.

	NOTE:  Be sure to update the "C" version of this file as
	       well -- they should always match.

	Name	Date		Description
	----	----		-----------
	doug	1/92		Initial version

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@


;GeoWorksGenAppGCNListType	etype	word, first GenApplicationMessages, 2
GeoWorksGenAppGCNListType	etype	word, FIRST_GEN_APP_GCN_LIST_TYPE, 2


	; GenToolControl/GenControl communication related

GAGCNLT_SELF_LOAD_OPTIONS			enum GeoWorksGenAppGCNListType
	; this list is for objects that don't need to receive
	; MSG_META_LOAD_OPTIONS on startup, but do need to receive
	; MSG_META_SAVE_OPTIONS.  MSG_META_SAVE_OPTIONS will be sent when the
	; GenApplication receives MSG_META_SAVE_OPTIONS.  This must be done by
	; the application, usually when a "Save Options" trigger is activated.
	; Objects on this list can be of any class, as MetaClass defines the
	; options behavior, though objects will most likely be of a Generic UI
	; class, as GenClass provides the .ini file behavior.
	;
	; Any controller not on the GAGCNLT_STARTUP_LOAD_OPTIONS list will
	; need to be placed on this list.
	;
	; A controller should not appear in both lists.
	;
	; Other objects on this list will be those that support options like
	; GenBooleanGroup, GenItemGroup, or a GenInteraction with
	; ATTR_GEN_INIT_FILE_PROPAGATE_TO_CHILDREN.  These should have the
	; appropriate ATTR_GEN_INIT_FILE_KEY.  The GenApplication object should
	; have an appropriate ATTR_GEN_INIT_FILE_CATEGORY.

GAGCNLT_GEN_CONTROL_NOTIFY_STATUS_CHANGE	enum GeoWorksGenAppGCNListType
; List for keeping GenToolControl up to date on the status of
; all the GenControl objects.  Status event NOT used for this list --
; all notification should be sent only.
;
; Data type:  NotifyGenControlStatusChange


	; Type for the GenEditControl (cut/copy/paste)
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_SELECT_STATE_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the selection state of the current
	; target object -- data type NotifySelectStateChange

GAGCNLT_EDIT_CONTROL_NOTIFY_UNDO_STATE_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the undo state of the current
	; process -- data type NotifyUndoStateChange

	; These types are high level types used for the text object
	; These types all use MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_TEXT_CHAR_ATTR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: VisTextNotifyCharAttrChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_PARA_ATTR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: VisTextNotifyParaAttrChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_TYPE_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: VisTextNotifyTypeChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_SELECTION_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: VisTextNotifySelectionChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_COUNT_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: VisTextNotifyCountChange
GAGCNLT_APP_TARGET_NOTIFY_STYLE_TEXT_CHANGE enum GeoWorksGenAppGCNListType
	; Sent when the current style could have changed (i.e. a different
	; style could be the current style) -- data type NotifyStyleChange
GAGCNLT_APP_TARGET_NOTIFY_STYLE_SHEET_TEXT_CHANGE enum GeoWorksGenAppGCNListType
	; Sent when the style sheet could have changed (i.e. a style was
	; created, deleted or modified) -- data type NotifyStyleSheetChange

	; The types below are low-level types for the text attribute
	; controllers in the text library
	; These types all use MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_TEXT_STYLE_CHANGE 	enum GeoWorksGenAppGCNListType
				; Data type: NotifyTextStyleChange
GAGCNLT_APP_TARGET_NOTIFY_FONT_CHANGE 		enum GeoWorksGenAppGCNListType
				; Data type: NotifyFontChange
GAGCNLT_APP_TARGET_NOTIFY_POINT_SIZE_CHANGE 	enum GeoWorksGenAppGCNListType
				; Data type: NotifyPointSizeChange
GAGCNLT_APP_TARGET_NOTIFY_FONT_ATTR_CHANGE 	enum GeoWorksGenAppGCNListType
				; Data type: NotifyFontAttrChange
GAGCNLT_APP_TARGET_NOTIFY_JUSTIFICATION_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: NotifyJustificationChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_FG_COLOR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: NotifyColorChange
GAGCNLT_APP_TARGET_NOTIFY_TEXT_BG_COLOR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: NotifyColorChange
GAGCNLT_APP_TARGET_NOTIFY_PARA_COLOR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: NotifyColorChange
GAGCNLT_APP_TARGET_NOTIFY_BORDER_COLOR_CHANGE	enum GeoWorksGenAppGCNListType
				; Data type: NotifyColorChange

GAGCNLT_APP_TARGET_NOTIFY_SEARCH_SPELL_CHANGE	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_SEARCH_REPLACE_CHANGE	enum GeoWorksGenAppGCNListType

	; Chart related

GAGCNLT_APP_TARGET_NOTIFY_CHART_TYPE_CHANGE	enum GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_CHART_GROUP_FLAGS	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_CHART_AXIS_ATTRIBUTES	enum GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_CHART_MARKER_SHAPE	enum GeoWorksGenAppGCNListType

	; GrObj related

GAGCNLT_APP_TARGET_NOTIFY_GROBJ_CURRENT_TOOL_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_GROBJ_BODY_SELECTION_STATE_CHANGE	\
						enum GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_GROBJ_AREA_ATTR_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_GROBJ_LINE_ATTR_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_GROBJ_TEXT_ATTR_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_STYLE_GROBJ_CHANGE enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_STYLE_SHEET_GROBJ_CHANGE enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_GROBJ_BODY_INSTRUCTION_FLAGS_CHANGE	\
						enum GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_GROBJ_GRADIENT_ATTR_CHANGE \
						enum GeoWorksGenAppGCNListType


	; Ruler related

GAGCNLT_APP_TARGET_NOTIFY_RULER_TYPE_CHANGE	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_RULER_GRID_CHANGE	enum GeoWorksGenAppGCNListType

GAGCNLT_TEXT_RULER_OBJECTS			enum GeoWorksGenAppGCNListType
	; List of all ruler objects

GAGCNLT_APP_TARGET_NOTIFY_RULER_GUIDE_CHANGE	enum GeoWorksGenAppGCNListType

	; VisBitmap related

GAGCNLT_APP_TARGET_NOTIFY_BITMAP_CURRENT_TOOL_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_BITMAP_CURRENT_FORMAT_CHANGE \
						enum GeoWorksGenAppGCNListType

	; Flat file library related

GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_FIELD_PROPERTIES_STATUS_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_FIELD_LIST_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_RCP_STATUS_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_FIELD_APPEARANCE_CHANGE \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_DUMMY_CHANGE_2 \
						enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_FLAT_FILE_DUMMY_CHANGE_3 \
						enum GeoWorksGenAppGCNListType

	; Spool library related

GAGCNLT_APP_NOTIFY_DOC_SIZE_CHANGE		enum GeoWorksGenAppGCNListType
				; Data type: NotifyPageSetupChange
GAGCNLT_APP_NOTIFY_PAPER_SIZE_CHANGE		enum GeoWorksGenAppGCNListType
				; Data type: NotifyPageSetupChange


	; Type for the GenViewControl
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_VIEW_STATE_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the selection state of the current
	; target object -- data type NotifyViewStateChange

GAGCNLT_CONTROLLED_GEN_VIEW_OBJECTS		enum GeoWorksGenAppGCNListType
	; List of all GenView objects that have the
	; ATTR_GEN_VIEW_INTERACT_WITH_CONTROLLER attribute
	;NOTE: If you aren't a GenView object, you shouldn't be on this list!

GAGCNLT_APP_TARGET_NOTIFY_INK_STATE_CHANGE	enum GeoWorksGenAppGCNListType
	; Used to notify controllers when ink objects come up

GAGCNLT_CONTROLLED_INK_OBJECTS			enum GeoWorksGenAppGCNListType
	; List of all Ink objects that have the "IF_CONTROLLED" bit set
	;NOTE: If you aren't an Ink object, you shouldn't be on this list!

	; Float library related

GAGCNLT_APP_TARGET_NOTIFY_PAGE_STATE_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the selection state of the current
	; target object -- data type NotifyPageStateChange

	; Type for the GenDocumentControl
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_DOCUMENT_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the state of the current target
	; document -- data type NotifyPageStateChange


	; Type for the GenDisplayControl
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_DISPLAY_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the state of the current target
	; display -- data type NotifyDisplayChange
GAGCNLT_APP_TARGET_NOTIFY_DISPLAY_LIST_CHANGE	enum GeoWorksGenAppGCNListType
	; List for information about the state of the current target
	; display -- data type NotifyDisplayChange

;-----------------------------------------------------------------------------
;	SPLINE LIBRARY NOTIFICATION LISTS
;-----------------------------------------------------------------------------
 
GAGCNLT_APP_TARGET_NOTIFY_SPLINE_MARKER_SHAPE	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_SPLINE_POINT		enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_SPLINE_POLYLINE	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_SPLINE_SMOOTHNESS	enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_SPLINE_OPEN_CLOSE_CHANGE enum \
						GeoWorksGenAppGCNListType

;-----------------------------------------------------------------------------
;	SPREADSHEET LIBRARY NOTIFICATION LISTS
;-----------------------------------------------------------------------------

GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_ACTIVE_CELL_CHANGE	enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetActiveCellChanged
GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_EDIT_BAR_CHANGE		enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetEditBarChanged
GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_SELECTION_CHANGE		enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetSelectionChanged

GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_CELL_WIDTH_HEIGHT_CHANGE	enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetCellWidthHeightChange

GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_DOC_ATTR_CHANGE	enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetDocAttrChange


GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_CELL_ATTR_CHANGE	enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetCellAttrChange
GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_CELL_NOTES_CHANGE	enum GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_DATA_RANGE_CHANGE enum GeoWorksGenAppGCNListType
	; data type -- NotifySSheetDataRangeChange

GAGCNLT_APP_TARGET_NOTIFY_TEXT_NAME_CHANGE	enum GeoWorksGenAppGCNListType
	; data type -- VisTextNotifyNameChange


GAGCNLT_FLOAT_FORMAT_CHANGE			enum GeoWorksGenAppGCNListType


GAGCNLT_DISPLAY_OBJECTS_WITH_RULERS		enum GeoWorksGenAppGCNListType

	; This list contains all GenDisplay's that have rulers

        ; Type for the GenImpexMapControl
	; This type uses MSG_META_NOTIFY_WITH_DATA_BLOCK

GAGCNLT_APP_TARGET_NOTIFY_APP_CHANGE		enum GeoWorksGenAppGCNListType
GAGCNLT_APP_TARGET_NOTIFY_LIBRARY_CHANGE	enum GeoWorksGenAppGCNListType

;------------------------------------------------------------------------------
;	UI NOTIFICATION LISTS
;------------------------------------------------------------------------------

GAGCNLT_WINDOWS					enum GeoWorksGenAppGCNListType
	; this list is for windowed objects on screen.  In your .ui/.goc file,
	; you would have any windows on this list that you want to appear when
	; the application starts up.  In most cases, this is just a GenPrimary.
	; When windows are opened, they are automatically added to this list.
	; The list is saved to state and used to re-open all windows that were
	; on-screen when the appliation shutdown.  The only message sent to
	; this list is MSG_META_UPDATE_WINDOW.  The classes of objects on this
	; list are GenPrimaryClass, GenDisplayClass, and GenInteractionClass.
	; GenDisplayGroup doesn't need to be on this list.

GAGCNLT_STARTUP_LOAD_OPTIONS			enum GeoWorksGenAppGCNListType
	; this list is for objects that need to receive MSG_META_LOAD_OPTIONS
	; on startup.  MSG_META_LOAD_OPTIONS is automatically sent by the UI
	; when the application starts up.  MSG_META_SAVE_OPTIONS will be sent
	; to the objects on the list when the GenApplication receives a
	; MSG_META_SAVE_OPTIONS.  This must be done by the application, usually
	; when a "Save Options" trigger is activated.  Objects on this list can
	; be of any class, as MetaClass defines the options behavior, though
	; objects will most likely be of a Generic UI class, as GenClass
	; provides the .ini file behavior.
	;
	; Certain controllers must be placed on this list:
	;	GenViewControl
	;
	; Other objects on this list will be those that support options like
	; GenBooleanGroup, GenItemGroup, or a GenInteraction with
	; ATTR_GEN_INIT_FILE_PROPAGATE_TO_CHILDREN.  These should have the
	; appropriate ATTR_GEN_INIT_FILE_KEY.  The GenApplication object should
	; have an appropriate ATTR_GEN_INIT_FILE_CATEGORY.

;------------------------------------------------------------------------------
;	CARD LIBRARY NOTIFICATION LISTS
;------------------------------------------------------------------------------
GAGCNLT_APP_TARGET_NOTIFY_CARD_BACK_CHANGE	enum GeoWorksGenAppGCNListType

;-----------------------------------------------------------------------------
;	*** TO BE MOVED LATER ***
;-----------------------------------------------------------------------------

GAGCNLT_NOTIFY_FOCUS_TEXT_OBJECT		enum GeoWorksGenAppGCNListType

GAGCNLT_NOTIFY_TEXT_CONTEXT			enum GeoWorksGenAppGCNListType

;------------------------------------------------------------------------------
;	HELP NOTIFICATION LISTS
;------------------------------------------------------------------------------

GAGCNLT_NOTIFY_HELP_CONTEXT_CHANGE 		enum GeoWorksGenAppGCNListType

;-----------------------------------------------------------------------------
;	*** TO BE MOVED LATER ***
;-----------------------------------------------------------------------------

GAGCNLT_FLOAT_FORMAT_INIT			enum GeoWorksGenAppGCNListType
GAGCNLT_ALWAYS_INTERACTABLE_WINDOWS		enum GeoWorksGenAppGCNListType
;
;	This is a list of windows that always want to be interactible (even
;	when modal windows are on screen).
;

GAGCNLT_USER_DO_DIALOGS				enum GeoWorksGenAppGCNListType
;
;	This is a list of all dialog boxes initiated via UserDoDialog.
;

GAGCNLT_MODAL_WIN_CHANGE			enum GeoWorksGenAppGCNListType
;
;	Objects that need notification upon modal window changes within
;	the application.
;

GAGCNLT_APP_TARGET_NOTIFY_SPREADSHEET_NAME_CHANGE enum GeoWorksGenAppGCNListType

GAGCNLT_CONTROLLERS_WITHIN_USER_DO_DIALOGS	enum GeoWorksGenAppGCNListType
;
;	Objects (like controllers) that will be in a UserDoDialog, but will
;	not be in the same block as the dialog (or will have children
;	that won't be in the same block as the dialog). Objects on this
;	list will get MSG_META_CHECK_IF_INTERACTABLE_OBJECT sent to them
;	when messages are sent via the thread that called UserDoDialog.
;

GAGCNLT_FOCUS_WINDOW_KBD_STATUS			enum GeoWorksGenAppGCNListType
;
;	When windows get the focus on pen systems, they send this out to
;	tell the floating keyboard what it should do.
;

GAGCNLT_APP_TARGET_NOTIFY_PAGE_INFO_STATE_CHANGE enum GeoWorksGenAppGCNListType
;
;	Objects that need notification of page info (size, margin) changes.
;

GAGCNLT_APP_TARGET_NOTIFY_CURSOR_POSITION_CHANGE enum GeoWorksGenAppGCNListType
	; data type -- VisTextCursorPositionChange

GAGCNLT_MAILBOX_SEND_CONTROL			enum GeoWorksGenAppGCNListType
;
;	Notifications directed to the MailboxSendControl object(s) for
;	the application.
;	Notification types: GWNT_MAILBOX_SEND_CONTEXT
;


GAGCNLT_APP_TARGET_NOTIFY_PAGE_NAME_CHANGE	enum GeoWorksGenAppGCNListType
; 	List for information about the current page name
; 	data type - NotifyPageNameChange

GAGCNLT_GAME_STATUS_CHANGE	enum	GeoWorksGenAppGCNListType

GAGCNLT_APP_TARGET_NOTIFY_TEXT_HYPERLINKABILITY_CHANGE	enum GeoWorksGenAppGCNListType
;
;	Notifications of changes in the hyperlinkability of the
;	current selection. The selection may be text or an object
;	embedded in the text through association with a C_GRAPHIC
;	character. 
;
;	Notification type: GWNT_TEXT_HYPERLINKABILITY_CHANGE
;

;------------------------------------------------------------------------------
;	CONTENT LIBRARY NOTIFICATION LISTS
;------------------------------------------------------------------------------

GAGCNLT_NOTIFY_CONTENT_CONTEXT_CHANGE 		enum GeoWorksGenAppGCNListType
;	List for information about a hyperlink being followed.
;	Navigation controller (ContentNavControlClass) sits on this
;	list so it can update its state. The Context (ContextControlClass),
;	Find (ContentFindControlClass), and Send (ContentSendControlClass)
;	controllers also sit on this list.
;
;	Notification type: GWNT_CONTENT_CONTEXT_CHANGE
;

GAGCNLT_NOTIFY_CONTENT_BOOK_CHANGE 		enum GeoWorksGenAppGCNListType
;	List for information about a new book being loaded. 
;	The Navigation (ContentNavControlClass), Context (ContextControlClass),
;	Find (ContentFindControlClass), and Send (ContentSendControlClass)
;	controllers sit on this list so they can update their UI based on
;	the features and tools set for the new book.
;
;	Notification type: GWNT_CONTENT_BOOK_CHANGE
;

;-----------------------------------------------------------------------------
;	ADDITIONAL HYPERLINK LIBRARY NOTIFICATION LISTS
;-----------------------------------------------------------------------------
GAGCNLT_APP_TARGET_NOTIFY_HYPERLINK_STATUS_CHANGE	enum GeoWorksGenAppGCNListType
;
;	Notifications of changes in the status of hyperlinks.
;	The Hyperlink controller sits on this list so it can update 
;	its state.
;
;	Notification type: GWNT_HYPERLINK_STATUS_CHANGE
;

;-----------------------------------------------------------------------------
;	NAVIGATE CONTROLLER NOTIFICATION LISTS
;-----------------------------------------------------------------------------
GAGCNLT_NOTIFY_NAVIGATE_ENTRY_CHANGE	enum GeoWorksGenAppGCNListType
;
;	Navigation controller (a NavigateControlClass object) sits on this
;	GCN list so it can update its state. 
;
;   	Notification type: GWNT_NAVIGATE_ENTRY_CHANGE
;

;-----------------------------------------------------------------------------
;	FOAM NOTIFICATION LIST
;-----------------------------------------------------------------------------
GAGCNLT_FOAM_NOTIFICATIONS	enum GeoWorksGenAppGCNListType
;
;	Foam apps and controllers sit on this GCN list so they can receive 
;	foam specific notifications.
;
;   	Notification types: GWNT_FOAM_AUTO_SAVE,
;		GWNT_FOAM_LOST/GAINED_FULL_SCREEN_EXCL, 
;		GWNT_FOAM_IGNORE_BRING_TO_TOP
;

;-----------------------------------------------------------------------------
;	FULL SCREEN EXCL NOTIFICATION LIST
;-----------------------------------------------------------------------------
GAGCNLT_FULL_SCREEN_EXCL_CHANGE	enum GeoWorksGenAppGCNListType
;
;	List of objects to receive MSG_META_LOST/GAINED_FULL_SCREEN_EXCL
;	when the application object receives these.  Normally, just the
;	GenApplication object receives these messages.
;
;	MSG_META_GAINED_FULL_SCREEN_EXCL is sent before default handling in
;	GenApplicationClass.  MSG_META_LOST_FULL_SCREEN_EXCL is sent
;	after default handling.
;

GAGCNLT_VIEWER_NOTIFICATIONS	enum GeoWorksGenAppGCNListType
;
;	Objects interested in the status of files being viewed or created
;	should hook themselves up to this list.
;
;	Notification types:	GWNT_VIEWER_DOCUMENT_STATUS_CHANGE
;

;-----------------------------------------------------------------------------
;	DATASTORE CHANGE NOTIFICATION LIST
;-----------------------------------------------------------------------------
GAGCNLT_NOTIFY_DATASTORE_CHANGE		enum GeoWorksGenAppGCNListType
;
;	A list for objects which care to know when a datastore file has
;	changed.
;
;	Notification types:	GWNT_DATASTORE_CHANGE
;


COMMENT @%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	Meta GCNList enums
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

	The kernel library's MetaClass supports its very own GCN
	(General Change Notification) system separate from the kernel's.
	Lists within this system are identified by a MGCNListType, whose
	enumerations are seperate from that of the kernel's GCN system.

	This section contains the enumerations of the GCNLT_type field for
	GCNListType used within a Meta for the case of
	GCNLT_manuf = MANUFACTURER_ID_GEOWORKS.

	NOTE:  Be sure to update the "C" version of this file as
	       well -- they should always match.

	Name	Date		Description
	----	----		-----------
	doug	1/92		Initial version

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@


;GeoWorksMetaGCNListType	etype	word, first MetaMessages, 2
GeoWorksMetaGCNListType	etype	word, FIRST_META_GCN_LIST_TYPE, 2

MGCNLT_ACTIVE_LIST				enum GeoWorksMetaGCNListType
	; this list is for objects that need to receive MSG_META_ATTACH,
	; MSG_META_DETACH, and/or MSG_META_QUIT.  Currently, this is only
	; certain controllers.  These controllers need MSG_META_ATTACH, so
	; they should be placed on the MGCNLT_ACTIVE_LIST list in the .ui/.goc
	; file.  Other objects may add themselves dynamically to this list, if
	; they only need to receive MSG_META_DETACH or MSG_META_QUIT.
	;
	; These are the current controllers that need to be on the list:
	;	GenToolControl
	;	GenDocumentControl
	;	GenDisplayControl
	;	TextRulerControl
	;	TabControl
	;
	; Aside from controller-specific reasons, a controller would need to
	; receive MSG_META_ATTACH (and thus would need to be on the active list)
	; if it is always interactable (GCBF_ALWAYS_INTERACTABLE) or if it is
	; always on its GCN lists (GCBF_ALWAYS_ON_GCN_LIST).
	;
	; Objects on this list may be of any class, as MSG_META_ATTACH,
	; MSG_META_DETACH, and MSG_META_QUIT are defined for MetaClass.

MGCNLT_APP_STARTUP				enum GeoWorksMetaGCNListType
	; this list is for objects that need to receive MSG_META_APP_STARTUP
	; and MSG_META_APP_SHUTDOWN so they know when the application has
	; been started or is about to exit, regardless of whether the
	; app will become/was available to the user. For example, the
	; GenDocumentControl needs to receive these messages so it can open
	; and manipulate a passed document even when the application is
	; launched in engine mode to perform some query on the document.
	;
	; Objects on this list may be of any class, as MSG_META_APP_STARTUP
	; and MSG_META_APP_SHUTDOWN are defined for MetaClass.


COMMENT @%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	VisContent GCNList enums
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

	The UI library's VisContentClass supports its very own GCN
	(General Change Notification) system separate from the kernel's.
	Lists within this system are identified by a GCNListType, whose
	enumerations are seperate from that of the kernel's GCN system.

	This section contains the enumerations of the VCGCNLT_type field for
	GCNListType used within a GenApplication for the case of
	GCNLT_manuf = MANUFACTURER_ID_GEOWORKS.

	NOTE:  Be sure to update the "C" version of this file as
	       well -- they should always match.

	Name	Date		Description
	----	----		-----------
	doug	1/92		Initial version

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@


;GeoWorksVisContentGCNListType	etype	word, first VisContentMessages, 2
GeoWorksVisContentGCNListType	etype	word, FIRST_VIS_CONTENT_GCN_LIST_TYPE, 2

VCGCNLT_TARGET_NOTIFY_TEXT_PARA_ATTR_CHANGE enum GeoWorksVisContentGCNListType
				; Data type: VisTextNotifyParaAttrChange

COMMENT @----------------------------------------------------------------------

DESCRIPTION:	Transfer item types

------------------------------------------------------------------------------@

; predefined formats for MANUFACTURER_ID_GEOWORKS

ClipboardItemFormat etype word
CIF_TEXT 		enum	ClipboardItemFormat
	; Null terminated text with formatting information.
	; (see TextTransferBlockHeader documentation in Objects/vTextC.def)

CIF_GRAPHICS_STRING 	enum	ClipboardItemFormat
	; Standard GEOS graphics string
	; (see Graphics String documentation for format -- GST_VMEM)

CIF_FILES 		enum	ClipboardItemFormat
	; Internal desktop format for direct-manipulation file operations
	; (only valid for quick-transfer operations)
	; 			** NOTE **
	;	This format passes a FileQuickTransfer block, the header and
	; content structures of which are defined in .Include/fmtool.def
	;	When using this format, make sure you pass the true diskhandle
	; of the transfer's files (the disk on which the directory containing
	; these files resides after all links have been resolved (use 
	; FileConstructActualPath)) in the CIFI_extra1 field.  CIFI_extra2 is
	; the remote flag, which should be set if ANY file in the transfer
	; is remote (if the FEA_PATH_INFO data doesn't have the
	; DPI_EXISTS_LOCALLY flag set).

CIF_SPREADSHEET 	enum	ClipboardItemFormat

CIF_INK			enum	ClipboardItemFormat
	; 

CIF_GROBJ		enum	ClipboardItemFormat
	; Format specific to the Graphic Object Library.

CIF_GEODEX		enum	ClipboardItemFormat

CIF_BITMAP		enum	ClipboardItemFormat
	; A huge bitmap. See GrCreateBitmap for more details on a huge bitmap.

CIF_SOUND_SYNTH		enum	ClipboardItemFormat
	; A multi-track description of a sound, musical in nature.

CIF_SOUND_SAMPLE	enum	ClipboardItemFormat
	; Sampled sound description.

CIF_ICON_LIST		enum	ClipboardItemFormat
	; Internal icon editor format for transferring groups
	; of icons between icon files.

CIF_FAX_FILE_PAGE_WITH_INK	enum	ClipboardItemFormat
	; Internal faxview format.  It is a tree that points to a huge array
	; containing a compressed fax page, and a db item holding the
	; pages ink.

COMMENT @----------------------------------------------------------------------

DESCRIPTION:	Text Object variable types

------------------------------------------------------------------------------@

VisTextVariableType	etype	word

VTVT_PAGE_NUMBER		enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_PAGE_NUMBER_IN_SECTION	enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_NUMBER_OF_PAGES		enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_NUMBER_OF_PAGES_IN_SECTION	enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_SECTION_NUMBER		enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_NUMBER_OF_SECTIONS		enum	VisTextVariableType
	; private data: first word is VisTextNumberType
	; vm chain: unused

VTVT_CREATION_DATE_TIME		enum	VisTextVariableType
	; private data: first word is DateTimeFormat
	; vm chain: unused
	; available only for large text objects

VTVT_MODIFICATION_DATE_TIME	enum	VisTextVariableType
	; private data: first word is DateTimeFormat
	; vm chain: unused
	; available only for large text objects

VTVT_CURRENT_DATE_TIME		enum	VisTextVariableType
	; private data: first word is DateTimeFormat
	; vm chain: unused
	; available only for large text objects

VTVT_STORED_DATE_TIME		enum	VisTextVariableType
	; private data: first word is DateTimeFormat, 2nd is FileDate, 3d
	;  is FileTime
	; vm chain: unused

VTVT_CONTEXT_PAGE		enum	VisTextVariableType
	; private data: first word is VisTextNumberType, 2nd is context token
	; vm chain: unused
	; available only in GeoWrite

VTVT_CONTEXT_PAGE_IN_SECTION	enum	VisTextVariableType
	; private data: first word is VisTextNumberType, 2nd is context token
	; vm chain: unused
	; available only in GeoWrite

VTVT_CONTEXT_SECTION		enum	VisTextVariableType
	; private data: first word is VisTextNumberType, 2nd is context token
	; vm chain: unused
	; available only in GeoWrite

VTVT_CONTEXT_NAME		enum	VisTextVariableType
	; private data: 1st word is unused, 2nd is context token
	; vm chain: unused
	; available only in GeoWrite

VTVT_HOTSPOT			enum	VisTextVariableType
	; private data: 1st word is x offset, 2nd is y offset, (relative
	; 	to the embedded graphic), 3rd is hotspot token
	; vm chain: gstring containing HotSpot description
	; available only in Studio


COMMENT @----------------------------------------------------------------------

DESCRIPTION:	Escape Codes for GR_ESCAPE GString Elements

------------------------------------------------------------------------------@
;
; This is the range of GString Escape code available for use by Geoworks.
; Numbers below this are reserved for GeoDraw, Clipboard and system use.
; See gstring.def for more details.
;
FIRST_GEOWORKS_GSTRING_ESCAPE_CODE	equ	4200h
LAST_GEOWORKS_GSTRING_ESCAPE_CODE	equ	7fffh

GeoworksGStringEscapeCode etype	word, FIRST_GEOWORKS_GSTRING_ESCAPE_CODE

GGSEC_HOTSPOT		enum	GeoworksGStringEscapeCode
	; used by Condo to support Hotspots stored in GStrings
	;


.assert (GeoworksGStringEscapeCode-1 le LAST_GEOWORKS_GSTRING_ESCAPE_CODE)
