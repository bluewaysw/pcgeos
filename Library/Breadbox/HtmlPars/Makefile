#
# THIS FILE HAS BEEN GENERATED AUTOMATICALLY.
#
# If you edit it, you will lose your changes, should it be regenerated.
#
GEODE           = htmlpars
HTMLCLAS        = htmlclas.goc
WWWTOOLS        = wwwtools.goc
HTMLEXPT        = htmlexpt.goc
HTMLPARS        = htmlpars.goc
UI_TO_RDFS      =
OBJS            = htmlclas.obj wwwtools.obj htmlexpt.obj htmlpars.obj
COMMON          = htmlsty.goh
MODULES         =
CMODULES        = htmlclas wwwtools htmlexpt htmlpars
SRCS            = $(HTMLCLAS) $(WWWTOOLS) $(HTMLEXPT) $(HTMLPARS) $(COMMON)
LOBJS           =
LIBOBJ          = $(DEVEL_DIR)/Include/$(GEODE).ldf
LIBNAME         = htmlpars

SYSMAKEFILE     = geode.mk

#include <geos.mk>
#include <gpath.mk>

#if exists(local.mk)
#include "local.mk"
#else
#include <$(SYSMAKEFILE)>
#endif

#if exists($(DEPFILE))
#include "$(DEPFILE)"
#endif
