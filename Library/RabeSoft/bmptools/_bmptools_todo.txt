- ApplyTransparencymask: Handling, wenn größe nicht passt


			maskFile, maskBitmap: handle of a monochrome bitmap that holds the new mask
 *				- if the bitmap also has a mask, picture bits and mask bits will be
 *				  combinded with AND, just as we draw the bitmap.
 --> Handling MaksBitmap komprimiert
 
 BTApplyTransparencyColor: Noch mal tesetn ob alle Kombinationen source = masked/notMasked, compressed / notCompresed
 	funktionieren (code auf einfaches BTCopyToBitmapWithMask() umgestellt)
 
 ! Alle Routinen test on paramter Null sind
 
Alle Bitmaptools  routinen auflisten Reihenfolge RscFile, desTFiole, srcBitmap
--> Neue routinen möglichst kompatibel machn
 
- SrcFile = 0 Handling für 
	BTApplyTransparencyMask 


#################### Anpassungen/fixes für vorhandene Routinen #####################################
- Support für 4 Bit für 	- clipping
