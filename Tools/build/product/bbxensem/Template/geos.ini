[system]
NTDEMO(primaryfsd = EC-dos(os2.geo))
PCDEMO(fs = EC-dos(ms4.geo))

[paths]
IF(buildfile == "ntbbx.build" && ec,ini = NETEC.INI)
IF(buildfile == "ntbbx.build" && !ec,ini = NET.INI)
IF(buildfile == "fg.build" && ec,ini = GFSEC.INI)
IF(buildfile == "fg.build" && !ec,ini = GFS.INI)

