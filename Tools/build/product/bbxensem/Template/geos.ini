[system]
primaryFSD = EC-dos(os2.geo)
fs = EC-dos(megafile.geo)

[gfs]
file = C:\IMAGE\GFS.IMG
drive = GFS
;bootstrapPath = C:\LOCALPC\GEOS ;make this the path that holds the image and is loaded as fist SP_TOP, then stripped once the GFS runs.
cacheFile = none

[paths]
top = GFS:\FG600
IF(ec,ini = C:\LOCALPC\GEOS\NETEC.INI)
IF(!ec,ini = C:\LOCALPC\GEOS\NET.INI)
sharedTokenDatabase = ..\PRIVDATA\SHARED
