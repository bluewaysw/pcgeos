[system]
NTDEMO(primaryfsd = EC-dos(os2.geo))
PCDEMO(fs = EC-dos(ms4.geo))

[paths]
IF(targetfile == "ntbbx.build" && ec,ini = NETEC.INI)
IF(targetfile == "ntbbx.build" && !ec,ini = NET.INI)
IF(targetfile == "ntbbxtools.build" && ec,ini = NETEC.INI)
IF(targetfile == "ntbbxtools.build" && !ec,ini = NET.INI)

IF(targetfile == "fg.build",ini = GFS.INI)
IF(targetfile == "fgtools.build",ini = GFS.INI)
