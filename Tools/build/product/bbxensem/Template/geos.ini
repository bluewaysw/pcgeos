[system]
primaryFSD = EC-dos(os2.geo)
fs = EC-dos(megafile.geo)

[paths]
top = C:\FG600 GFS:\FREEGEOS
IF(ec,ini = C:\FG600\NETEC.INI)
IF(!ec,ini = C:\FG600\NET.INI)
sharedTokenDatabase = ..\PRIVDATA\SHARED

[gfs]
file = C:\FG600\GFS.IMG
drive = GFS
; bootstrapPath holds the path to the
; image that is also loaded as the fist
; SP_TOP and then removed once the GFS runs
bootstrapPath = C:\FG600
cacheFile = none

