Requirements:
	- GDT
	- IDT
	- Syscall relocation
	- Storage of SP for thread on system call
	- Storage of jump vectors into proper locations for notification
	  by the kernel
	- Call kernel for stub
	- Switch into and out of protected mode
	- Handle interrupts when in protected mode
		- stub's interrupts (step, NMI, INT 3, exceptions)
		- user interrupts (set as indicated by debugger)
	- Load stub into memory
	- Load kernel into memory
