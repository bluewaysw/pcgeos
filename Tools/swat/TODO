Wed Apr 10 11:50:21 1991  Adam de Boor  (adam at neon)

	* deal with based pointers (somehow)

	* deal with all the C non-assignment operators and casting.

	* C near pointers need to be referenced off ds (if they're local
	variables) or just DGROUP (if they're static variables).

